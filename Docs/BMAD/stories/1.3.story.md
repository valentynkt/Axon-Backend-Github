# Story 1.3: Domain Aggregates & Invariants (Unit-Level)

## Status
Draft

## Story
**As a** domain engineer,  
**I want** aggregates and VOs enforcing invariants,  
**so that** business rules are explicit and testable without infra.

## Acceptance Criteria
1. Aggregates enforce: one verified & signing owner; verified-first defaults; idempotent link/update semantics; risk tier mapping (product ↔ wire).
2. **No-op guard:** Reapplying the same `riskTier` or the same default **MUST NOT** persist changes (no `updated_at` bump; ETag unchanged).
3. Unit tests cover linking, idempotency, default assignment/change, conflict detection, and no-op behavior.

## Integration Verification
- IV1: No EF types leak into domain/app layers.
- IV2: Result pattern for expected failures (no exceptions for control flow).
- IV3: Branch coverage ≥80% on invariants.

## Tasks / Subtasks
- [ ] Task 1: Enhance AxonPrincipal Aggregate Root (AC: 1, 2)
  - [ ] Implement verified-first defaults enforcement in `ApplyChainDefault` method
  - [ ] Add idempotent `LinkWalletOwnership` method with conflict detection
  - [ ] Add `UpdateRiskTier` method with no-op guard (same value = no changes)
  - [ ] Add risk tier mapping methods (wire ↔ domain enum conversion)
  - [ ] Ensure all invariants are enforced at domain level without EF dependencies

- [ ] Task 2: Enhance Wallet Aggregate (AC: 1)
  - [ ] Implement ownership state validation in `LinkToOwner` method
  - [ ] Add `CanBeLinked` validation logic for signing vs watchOnly conflicts
  - [ ] Ensure global uniqueness checks are domain-aware (delegated to repository)
  - [ ] Add wallet normalization and validation in `Address` value object

- [ ] Task 3: Implement Core Value Objects and Entities (AC: 1)
  - [ ] Enhance `Address` value object with normalization and validation
  - [ ] Implement `ChainId` value object with supported chain validation
  - [ ] Create `ProviderType` enum with Dynamic.xyz + future SIWS support
  - [ ] Enhance `WalletOwnership` entity with status transitions and validation rules
  - [ ] Create `PrincipalChainDefault` entity with verified-signing enforcement

- [ ] Task 4: Implement Domain Result Types and Error Handling (IV1, IV2)
  - [ ] Create domain-specific error types in `IdentityDomainErrors` class
  - [ ] Ensure all domain operations return `Result<T, Error>` patterns
  - [ ] Add conflict detection results (no exceptions for business rule violations)
  - [ ] Implement privacy-safe error details (no principal ID exposure in conflicts)

- [ ] Task 5: Create Comprehensive Unit Tests (AC: 3, IV3)
  - [ ] Test verified-first defaults: only verified+signing wallets can be defaults
  - [ ] Test ownership linking: one verified+signing owner per wallet invariant
  - [ ] Test idempotency: reapplying same operations produces no changes
  - [ ] Test no-op guards: same risk tier or default values don't trigger updates
  - [ ] Test conflict detection: proper domain errors when invariants violated
  - [ ] Test risk tier mapping: product terms ↔ wire enum ↔ domain enum
  - [ ] Achieve ≥80% branch coverage on all invariant logic

- [ ] Task 6: Domain Events Implementation (IV2)
  - [ ] Implement `PrincipalChangedEvent` for risk tier changes
  - [ ] Implement `OwnershipChangedEvent` for wallet linking/unlinking
  - [ ] Implement `WalletChangedEvent` for wallet modifications
  - [ ] Ensure events are raised only when actual state changes occur (no-op aware)

## Dev Notes

### Previous Story Insights
From Story 1.2 completion:
- JWT validation and rate limiting infrastructure is production-ready
- Authentication endpoints exist but need proper domain logic integration
- MediatR command/query handlers are structured but need aggregate interactions
- Infrastructure layer (DynamicAuthService) is complete and robust

### Domain Model Architecture Context
[Source: architecture.md#119-156]

**AxonPrincipal (Aggregate Root)**:
- **Purpose**: Canonical identity (human or service). Enforces invariants for credential uniqueness, wallet ownership, and chain defaults.
- **Key Attributes**: 
  - `Id: AxonId` (ULID, char(26))
  - `Type: PrincipalType` (`Human` | `Service`)
  - `RiskTier: RiskTier` (internal enum; wire = `low|medium|high`)
  - `Credentials: IdentityCredential[]` (unique by provider, issuer, subject)
  - `Ownerships: WalletOwnership[]` (access mode, status)
  - `ChainDefaults: Map<chainId, WalletId>` (must be verified + signing)

**Wallet (Aggregate)**:
- **Purpose**: Globally unique on-chain wallet (`chain`,`address`). Independent catalog entry until linked.
- **Key Attributes**:
  - `Id: WalletId` (ULID, char(26))
  - `ChainId: string` (e.g., `"solana"`)
  - `Address: Address` (normalized, validated)
  - `FirstSeenAt / LastSeenAt: DateTimeOffset`

### Critical Domain Invariants
[Source: architecture.md#147-155]

1. **Global Wallet Uniqueness**: `(chainId, address)` unique in `wallet`.
2. **Single Verified Signing Owner**: At most one `Principal` has **verified & signing** ownership for a wallet (partial-unique index).
3. **No Silent Reassignments**: Attempts to link a wallet already owned (verified & signing) by another principal → **409 Conflict**.
4. **Idempotent Exchange**: Reprocessing the same credential bundle does not duplicate or contradict prior state.
5. **Defaults Verified-First**: Chain default must reference a **verified** `signing` ownership.

### Architectural Patterns
[Source: architecture.md#75-82]

- **Clean Architecture**: Domain layer is pure C# with no infrastructure dependencies
- **DDD**: `AxonPrincipal` aggregate root enforces invariants for credentials, wallet ownership, defaults
- **Repository Pattern**: Application depends on interfaces; Infra implements with EF Core & compiled queries
- **Result Pattern**: Use `Result<TSuccess, TError>` for expected business outcomes

### Risk Tier Mapping Requirements
[Source: architecture.md#113, prd.md#397-400]

**Wire Enum**: `low | medium | high` (API contract)
**Product Terms**: `conservative | balanced | aggressive` (UI/UX terms)
**Domain Enum**: Internal `RiskTier` enum for business logic

**Mapping**: `conservative→low`, `balanced→medium`, `aggressive→high`

### File Locations
[Source: architecture.md#655-698]

**Domain Structure**:
```
src/Modules/Identity/Domain/
├── Aggregates/
│   ├── AxonPrincipal/
│   │   ├── AxonPrincipal.cs              # Primary aggregate root
│   │   ├── AxonPrincipal.Commands.cs     # Command methods (Link, Update, etc.)
│   │   └── AxonPrincipal.Queries.cs      # Query/validation methods
│   └── Wallet/
│       ├── Wallet.cs                     # Wallet aggregate
│       ├── Wallet.Commands.cs            # Wallet command methods
│       └── Wallet.Queries.cs             # Wallet query methods
├── Entities/
│   ├── IdentityCredential.cs             # Credential entity
│   ├── WalletOwnership.cs                # Ownership relationship entity
│   └── PrincipalChainDefault.cs          # Chain default entity
├── ValueObjects/
│   ├── Address.cs                        # Wallet address with validation
│   ├── ChainId.cs                        # Blockchain identifier
│   └── ProviderType.cs                   # Auth provider enum
├── Enums/
│   ├── RiskTier.cs                       # Risk posture enum
│   ├── PrincipalType.cs                  # Human vs Service
│   ├── AccessMode.cs                     # Signing vs WatchOnly
│   └── OwnershipStatus.cs                # Pending, Verified, Revoked
└── Errors/
    ├── IdentityDomainErrors.cs           # Domain error definitions
    └── WalletDomainErrors.cs             # Wallet-specific errors
```

### No-Op Guard Implementation Requirements
[Source: prd.md#298, prd.md#421]

**Critical**: Reapplying the same `riskTier` or the same default **MUST NOT** persist changes:
- No `updated_at` timestamp bump
- ETag must remain unchanged  
- Domain methods should detect and skip no-op operations
- Unit tests must verify this behavior explicitly

### Testing Requirements
[Source: architecture.md#631-650]

**Unit Testing Strategy**:
- **Framework**: NUnit + Shouldly
- **Focus**: Domain aggregates invariants (ownership uniqueness, verified-first defaults, idempotency)
- **Coverage Target**: ≥80% branch coverage on invariant logic
- **Test Categories**:
  - Happy path scenarios for all aggregate operations
  - Conflict/edge cases for invariant violations
  - No-op guards verification
  - Risk tier mapping round-trip tests

**Test File Locations**:
```
tests/Modules/Identity/Domain/
├── Aggregates/
│   ├── AxonPrincipalTests.cs             # Primary aggregate tests
│   └── WalletTests.cs                    # Wallet aggregate tests  
├── Entities/
│   ├── WalletOwnershipTests.cs           # Ownership entity tests
│   └── IdentityCredentialTests.cs       # Credential entity tests
└── ValueObjects/
    ├── AddressTests.cs                   # Address validation tests
    └── ChainIdTests.cs                   # Chain ID validation tests
```

### Current State Analysis
Based on existing source tree:
- Basic `AxonPrincipal` aggregate exists with core structure
- `Wallet` aggregate has basic implementation
- Core entities (`WalletOwnership`, `IdentityCredential`) are implemented
- Value objects (`Address`, `ChainId`) exist but may need enhancement
- Domain enums are defined but may need mapping methods
- Domain events exist but need proper integration
- Unit test structure exists but needs comprehensive invariant coverage

### Technical Constraints
[Source: architecture.md#618-627]

- **Pure Domain**: No EF types leak into domain/app layers (IV1)
- **Result Pattern**: Business failures return `Result`; do not throw for expected states (IV2)  
- **ULID IDs**: Generate ULID; store as `CHAR(26)`; never expose database surrogates
- **Modern C#**: File-scoped namespaces, records for DTOs, nullable reference types
- **Privacy**: No contact identifiers persisted or logged; tests must assert this

## Testing

### Testing Standards
[Source: architecture.md#631-650]

**Test File Locations**: 
- `tests/Modules/Identity/Domain/` - Domain unit tests
- Follow existing project structure with separate test files per aggregate/entity

**Test Standards**:
- **Framework**: NUnit + Shouldly assertion library
- **Naming**: Test methods should clearly describe the scenario and expected outcome
- **Structure**: Arrange-Act-Assert pattern with clear test sections
- **Coverage**: Target ≥80% branch coverage for all invariant logic

**Testing Frameworks and Patterns**:
- Use `Shouldly` for fluent assertions: `result.ShouldBe(expected)`
- Use `[Test]` attribute for test methods, `[TestCase]` for parameterized tests
- Use `[SetUp]` and `[TearDown]` for test initialization and cleanup
- Create test builders/factories for complex domain objects

**Specific Testing Requirements for This Story**:
- **Invariant Testing**: Every domain invariant must have explicit test coverage
- **No-Op Testing**: Verify that applying same values doesn't trigger state changes
- **Conflict Testing**: Test all conflict scenarios return proper domain errors
- **Mapping Testing**: Verify risk tier mappings work correctly in both directions
- **Edge Case Testing**: Test boundary conditions and invalid input scenarios

## Change Log
| Date | Version | Description | Author |
|------|---------|-------------|--------|
| 2025-01-09 | 1.0 | Initial story creation from PRD Epic 1.3 | Scrum Master (Bob) |

## Dev Agent Record
*This section will be populated by the development agent during implementation*

### Agent Model Used
*To be filled by dev agent*

### Debug Log References
*To be filled by dev agent*

### Completion Notes
*To be filled by dev agent*

### File List  
*To be filled by dev agent*

## QA Results
*This section will be populated by the QA agent after story completion*