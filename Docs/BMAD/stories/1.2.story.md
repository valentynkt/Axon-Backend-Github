# Story 1.2: JWT Validation & Rate Limiting Skeleton

## Status
Draft

## Story
**As a** platform engineer,  
**I want** JWT validation (JWKS rotation) and rate-limited API skeleton,  
**so that** calls are authenticated and `/auth/exchange` is protected from abuse.

## Acceptance Criteria
1. `JwtBearerHandler` + `ConfigurationManager` wired; issuer/audience, signature, expiry, skew enforced; JWKS cache with sensible TTL.
2. ASP.NET Core RateLimiter: `/auth/exchange` at **10 rpm/IP** returning `Retry-After`, `X-RateLimit-Remaining`, `X-RateLimit-Reset`.
3. Placeholder endpoints secured: **401** without token; **200** on mock handler with valid token.

## Integration Verification
- IV1: Global headers/security unaffected for existing modules.
- IV2: Auth failures logged safely; no secrets.
- IV3: Limiter overhead negligible at low QPS.

## Tasks / Subtasks
- [ ] Task 1: Configure JWT Bearer Authentication (AC: 1)
  - [ ] Add Microsoft.AspNetCore.Authentication.JwtBearer package
  - [ ] Configure JwtBearerOptions with issuer/audience validation [Source: architecture.md#607]
  - [ ] Set up ConfigurationManager for JWKS endpoint with automatic key rotation [Source: architecture.md#185]
  - [ ] Configure JWKS cache with provider-aligned TTL (~1h) [Source: architecture.md#101, prd.md#101]
  - [ ] Add JWT validation middleware to API pipeline in Program.cs [Source: source-tree.md#78]

- [ ] Task 2: Implement Rate Limiting for /auth/exchange (AC: 2)
  - [ ] Add Microsoft.AspNetCore.RateLimiting package
  - [ ] Configure rate limiter policy: 10 rpm per IP [Source: prd.md#88]
  - [ ] Add rate limiting middleware to pipeline
  - [ ] Configure rate limit headers: Retry-After, X-RateLimit-Remaining, X-RateLimit-Reset [Source: architecture.md#570]
  - [ ] Apply rate limit policy specifically to /auth/exchange endpoint

- [ ] Task 3: Create Placeholder Auth Endpoints (AC: 3)
  - [ ] Create /auth/exchange POST endpoint in src/Api/Endpoints/V1/Auth/ [Source: source-tree.md#20-28]
  - [ ] Create /auth/me GET endpoint in src/Api/Endpoints/V1/Auth/
  - [ ] Implement mock handlers returning 200 OK for valid tokens
  - [ ] Configure endpoints to return 401 Unauthorized without valid JWT
  - [ ] Add FastEndpoints configuration for auth endpoints

- [ ] Task 4: Configure External Services Integration (AC: 1)
  - [ ] Set up HttpClient for Dynamic.xyz JWKS endpoint [Source: external-services-integration.md#14-30]
  - [ ] Create DynamicAuthService wrapper with simple validation method [Source: external-services-integration.md#17-29]
  - [ ] Configure service in DI container with appropriate lifetime
  - [ ] Add health check for Dynamic.xyz JWKS endpoint availability [Source: external-services-integration.md#44]

- [ ] Task 5: Add Security Configuration and Logging (IV1, IV2)
  - [ ] Configure CORS policies for Axon frontend origins [Source: architecture.md#102]
  - [ ] Add security headers (HSTS, X-Content-Type-Options, etc.) [Source: architecture.md#611]
  - [ ] Implement structured logging for auth failures without exposing secrets [Source: coding-standards.md#620]
  - [ ] Add correlation IDs for request tracing [Source: tech-stack.md#27]

- [ ] Task 6: Create Integration Tests (IV3)
  - [ ] Test JWT validation with valid/invalid/expired tokens
  - [ ] Test rate limiting behavior and header responses
  - [ ] Test endpoint security (401 without token, 200 with valid token)
  - [ ] Verify no performance impact on existing modules
  - [ ] Test JWKS key rotation handling

## Dev Notes

### Previous Story Insights
From Story 1.1 completion:
- Core Domain layer is built and tested (41 tests passing)
- EF Core configurations and migration infrastructure ready
- Identity module structure established in modular monolith pattern
- Test suite compilation errors resolved during QA review

### Technical Architecture Context

**Authentication Flow** [Source: architecture.md#181-186]:
- **Stateless Design**: No server-issued tokens/cookies; validate provider JWT on every request
- **JWKS Validation**: ConfigurationManager with automatic key rotation and caching
- **Provider**: Dynamic.xyz as primary authentication provider
- **Claims**: Validate `iss`, `aud` (if configured), `sub`, expiration, signature

**Rate Limiting Requirements** [Source: prd.md#88, architecture.md#578-582]:
- **Policy**: `/auth/exchange` limited to 10 requests/min per IP
- **Implementation**: ASP.NET Core RateLimiter with token bucket
- **Headers**: Include `X-RateLimit-Remaining`, `X-RateLimit-Reset`, `Retry-After` on 429
- **Scope**: `/auth/me` has light/no rate limiting (read path)

### API Specifications
[Source: architecture.md#270-405]

**Endpoints to implement**:
- `POST /auth/exchange` - Rate limited, JWT protected
- `GET /auth/me` - JWT protected, light rate limiting

**Error Responses**:
- `400/401` - Malformed/invalid/expired JWT
- `429` - Rate limit exceeded with retry headers
- `500` - Unexpected errors

### File Locations
Based on modular monolith structure [Source: source-tree.md#16-52]:

```
src/Api/
├── Endpoints/V1/Auth/
│   ├── ExchangeEndpoint.cs        # POST /auth/exchange placeholder
│   └── MeEndpoint.cs              # GET /auth/me placeholder
├── Configuration/
│   ├── AuthenticationConfiguration.cs  # JWT + rate limiting setup
│   └── ServiceRegistration.cs     # DI container registration
├── Services/
│   └── DynamicAuthService.cs      # Dynamic.xyz integration wrapper
└── Program.cs                     # Middleware pipeline configuration
```

### External Services Integration
[Source: external-services-integration.md#6-36]

**Dynamic.xyz Integration Pattern**:
- Use 80/20 rule - leverage their JWKS, validation, and caching
- Simple wrapper service with single validation method
- No custom JWT logic - pure passthrough pattern
- Fail fast with simple error handling

### Technical Constraints
- **.NET 10** with modern C# patterns [Source: coding-standards.md#10-17]
- **FastEndpoints** for API implementation [Source: tech-stack.md#5]  
- **Result Pattern** for error handling - no exceptions for business logic [Source: coding-standards.md#18-35]
- **File-scoped namespaces** and modern C# features required
- **Structured logging** with OpenTelemetry integration [Source: tech-stack.md#25-28]

### Security Requirements
[Source: architecture.md#605-615]
- **HTTPS only** with HSTS
- **CORS** restricted to Axon frontend origins
- **No secrets in logs** - structured logging without PII
- **Security headers** at application level
- **JWT validation**: iss, aud, sub, exp, signature verification

### Testing Requirements
[Source: architecture.md#631-650, coding-standards.md#77-81]
- **Unit Tests**: NUnit + Shouldly for authentication logic
- **Integration Tests**: Real HTTP requests with test JWT tokens  
- **Performance Tests**: Verify rate limiting overhead is negligible
- **Security Tests**: Validate 401/429 responses and header behavior
- **Test Coverage**: Target ~80% for authentication components

## Change Log
| Date | Version | Description | Author |
|------|---------|-------------|--------|
| 2025-01-09 | 1.0 | Initial story creation from PRD Epic 1.2 | Scrum Master |