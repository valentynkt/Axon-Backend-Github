{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "AllowedHosts": "*",
  "ConnectionStrings": {
    "DefaultConnection": "Host=${POSTGRES_HOST:localhost};Port=${POSTGRES_PORT:5432};Database=${POSTGRES_DATABASE:axon_chat_dev};Username=${POSTGRES_USERNAME:postgres};Password=${POSTGRES_PASSWORD:postgres}"
  },
  "Chat": {
    "OpenAi": {
      "ApiKey": "${OPENAI_API_KEY:}",
      "Model": "o4-mini-2025-04-16",
      "MaxTokens": 4000,
      "TimeoutSeconds": 30,
      "Temperature": 0.7,
      "McpEnabled": true
    },
    "McpServers": {
      "Servers": {
        "CoinGecko": {
          "Enabled": true,
          "ServerUrl": "https://mcp.api.coingecko.com/sse"
        }
      }
    }
  }
}
